pipeline {
    agent any

    stages {
        stage('Git-webhook') {
            steps {
               git 'https://github.com/sankaranarayananmurali/webhook-repo.git'
            }
        }
    }
}
